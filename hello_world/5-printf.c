#include <stdio.h>
/**
 *main - entry point
 *Description:
 *with printf i need to add \n to make a new line at the end of my prints
 *Return: tell me with a 0 if the code work
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
